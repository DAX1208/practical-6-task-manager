import React from "react";

function Footer() {
    return (
        <footer role="contentinfo">
            <p>Practical 6 — Full Stack Integration with Code Splitting & Async Loading</p>
        </footer>
    );
}

export default React.memo(Footer);
